<UserControl x:Class="ExamDispatcher.Views.Questions.ShortAnswerQuestion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="580" d:DesignWidth="770">
    <DockPanel Margin="10" LastChildFill="True">
        <Label DockPanel.Dock="Top" FontSize="25" HorizontalAlignment="Center">Short Answer Question</Label>
        <Button Command="{Binding SaveCommand}" DockPanel.Dock="Bottom" Content ="Save"/>
        <StackPanel DockPanel.Dock="Bottom">
            <Label DockPanel.Dock="Top">Question:</Label>
            <TextBox Text="{Binding ExamQuestion}" Margin="5" DockPanel.Dock="Top" TextWrapping="Wrap"></TextBox>
            <Label DockPanel.Dock="Top">SuggestedAnswer:</Label>
            <TextBox Text="{Binding ExamAnswer}" Margin="5" DockPanel.Dock="Top" TextWrapping="Wrap"></TextBox>
        </StackPanel>
    </DockPanel>
</UserControl>
